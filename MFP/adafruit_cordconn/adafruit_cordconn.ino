#include <bluefruit.h>
//#include <SoftwareSerial.h>

// const int RX = 15;
// const int TX = 16;

//SoftwareSerial ccSerial(RX, TX); //(RX, TX)
void setup() {
  // put your setup code here, to run once:
  // ccSerial.begin(9600);
  // // Wait for Serial1 to initialize
  // while (!ccSerial) {
  //   delay(10);
  // }
}
// char c = ' ';
void loop() {
  // put your main code here, to run repeatedly:
  //Reads
  // if(ccSerial.available())
  // {
  //   c = ccSerial.read();
  //   Serial.write(c);
  // }

  // if(Serial.available()) {
  //   c = Serial.read();

  //   ccSerial.write(c);
  // }

  //Writes (comment this out if you just want to read)
  // ccSerial.write("hi");
  // delay(500);
}
